apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3
  namespace: app3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app3
  template:
    metadata:
      labels:
        app: app3
    spec:
      containers:
      - name: app3
        image: yeasy/simple-web
        ports:
        - containerPort: 8080

---

apiVersion: v1
kind: Service
metadata:
  name: app3
  namespace: 
spec:
  selector:
    app: app3
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

---     

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-app3
spec:
  rules:
  - host: "app3.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: app3
            port:
              number: 80