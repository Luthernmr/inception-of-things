apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: app2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app2
  template:
    metadata:
      labels:
        app: app2
    spec:
      containers:
      - name: app2
        image: yeasy/simple-web
        ports:
        - containerPort: 8080

---

apiVersion: v1
kind: Service
metadata:
  name: app2
  namespace: 
spec:
  selector:
    app: app2
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

---     

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-app2
spec:
  rules:
  - host: "app2.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: app2
            port:
              number: 80